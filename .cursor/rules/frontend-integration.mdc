---
description: 
globs: 
alwaysApply: false
---
# Frontend Integration Patterns

Best practices for integrating the DocuVault API in frontend applications.

## Authentication Integration
- Use JWT for traditional web applications
- Use SIWE for web3-enabled applications
- Store tokens securely (httpOnly cookies or secure storage)
- Implement automatic token refresh

## API Request Helper
Create a helper for consistent API requests:

```typescript
async function apiRequest(
  endpoint: string, 
  options: { 
    method?: string, 
    body?: any, 
    token?: string 
  } = {}
) {
  const { method = 'GET', body, token } = options;
  
  const headers: Record<string, string> = {
    'Content-Type': 'application/json'
  };
  
  if (token) {
    headers.Authorization = `Bearer ${token}`;
  }
  
  const response = await fetch(`/api/v1/${endpoint}`, {
    method,
    headers,
    body: body ? JSON.stringify(body) : undefined
  });
  
  const data = await response.json();
  
  if (!data.success) {
    throw new Error(data.error.message);
  }
  
  return data.data;
}
```

## DID Management
- Create DIDs after user authentication
- Cache DID resolutions to reduce API calls
- Implement proper error handling for DID operations

Reference: [apps/api/docs/dev-docs/frontend-api-reference.md](mdc:apps/api/docs/dev-docs/frontend-api-reference.md)
